#include "main.h"

/**
 *_strlen - Returns the length of a string
 *@s: is a parameter
 *Return: The length "i"
 */
int _strlen(char *s)
{
	int i;

		for (i = 0; s[i] != '\0'; i++)
		{
		}
		return (i);
}
