#include <stdio.h>

/**
 *main - prints the numbers from 0 to 9 using putchar
 *Return: 0 if success
 */
int main(void)
{
		int n = 48;
			while (n < 58)
		{
				putchar(n);
				n++;
		}
				putchar('\n');
			return (0);
}
